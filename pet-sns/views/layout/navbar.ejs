<div class="navbar-menu">
    <div style="font-weight: bolder; font-size: 1.5rem;">
        <a style="color:#000000; text-decoration: none;" href="#"></a>
    </div>
    <div class="auth_wrap">
        <% if(isAuthenticated.username){ %>
            <button onclick="location.herf='/posts/upload'" style="margin-right:10px" class="ui button">글쓰기</button>
            <form action="/auth/logout" method="POST">
                <button type="submit" style="margin-top:0" class="ui secondary button">로그아웃</button>
            </form>
            <% } else { %>
                <button onclick="location.herf='/auth/login'" style="width: 100%; margin-top: 0;"
                    class="ui secondary button">
                    로그인</button>
                <% } %>
    </div>
</div>